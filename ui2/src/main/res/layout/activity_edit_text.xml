<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_edit_text"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="net.devwiki.ui.edit.EditTextActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/one_input_btn"
            android:text="@string/one_input_btn"
            style="@style/button_item_style"/>

        <Button
            android:id="@+id/one_input_all_btn"
            android:text="@string/one_input_all_btn"
            style="@style/button_item_style"/>

        <Button
            android:id="@+id/one_input_all2_btn"
            android:text="@string/one_input_all2_btn"
            style="@style/button_item_style"/>

        <Button
            android:id="@+id/two_input_btn"
            android:text="@string/two_input_btn"
            style="@style/button_item_style"/>

        <Button
            android:id="@+id/two_input2_btn"
            android:text="@string/two_input2_btn"
            style="@style/button_item_style"/>

        <Button
            android:id="@+id/two_input_all_btn"
            android:text="@string/two_input_all_btn"
            style="@style/button_item_style"/>
    </LinearLayout>
</ScrollView>
